# Use persistent storage for Grafana in AWS
apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
  namespace: ai-finops
spec:
  template:
    spec:
      volumes:
        - name: storage
          persistentVolumeClaim:
            claimName: grafana-storage