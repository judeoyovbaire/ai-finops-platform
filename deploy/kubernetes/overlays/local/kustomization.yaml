apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai-finops

resources:
  - ../../base
  - mock-gpu-metrics.yaml

# For local development without real GPUs, use mock metrics
patches:
  - path: dcgm-disable-patch.yaml

labels:
  - pairs:
      environment: local